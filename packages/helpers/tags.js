/**
 * Tags Helper
 *
 * @param {object} data - the data we are filtering
 * @param {object} options - filter options
 */

module.exports = (data, options) => {
    console.log('testing');
}

module.exports.ALLOWED_OPTIONS = ['separator'];
